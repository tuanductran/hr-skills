# .claude-plugin

This directory contains generated Claude marketplace plugin artifacts.

- `marketplace.json` is generated by `bun run sync` (or `packages/hr-skills-build` sync task).
- This placeholder ensures documentation and tooling referencing `.claude-plugin/marketplace.json` find a valid file during development.

When you run the repository `sync` task, `marketplace.json` will be updated with real skill metadata.
