# Discord Developer Portal → your application → Bot → Token
DISCORD_TOKEN=

# Discord Developer Portal → your application → General Information → Application ID
DISCORD_CLIENT_ID=

# Optional: a guild (server) ID for instant command deployment during development.
# Leave empty to deploy commands globally (can take up to an hour to propagate).
DISCORD_GUILD_ID=
