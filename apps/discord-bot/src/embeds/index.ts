/**
 * Discord embed builders, one file per embed kind so each stays easy to
 * find and change without touching unrelated embeds.
 */
export { buildSkillListEmbed } from './list.ts';
export { buildPlanEmbed } from './plan.ts';
export { buildSkillEmbed } from './skill.ts';
export { buildStatsEmbed } from './stats.ts';
